build/
vcpkg_installed/
.vs/
*.user
compile_commands.json

# Compiler output artifacts (bronze-built programs — the host copy, the
# module beside it and the staged runtime — land in the working tree during
# testing and benchmarking; never commit them)
*.exe
*.dll
*.so
*.dylib
*.obj
*.pdb
*.ilk
*.lib
bench/results.jsonl
