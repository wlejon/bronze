build/
vcpkg_installed/
.vs/
*.user
compile_commands.json

# Compiler output artifacts (bronze-built exes and intermediates land in
# the working tree during testing; never commit them)
*.exe
*.obj
*.pdb
*.ilk
*.lib
