#pragma once

// The generated-code ABI is declared once, in pure C, in the abi module —
// see src/abi/bronze_abi.h for the registry and the rationale. This
// header exists so runtime code keeps a bronze-local include.
#include "abi/bronze_abi.h"
